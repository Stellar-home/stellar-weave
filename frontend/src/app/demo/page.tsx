import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Weave Demo — Social Graph on Stellar Testnet",
  description:
    "Connect a wallet, register an on-chain profile, and follow other profiles — live on Stellar Testnet.",
};

export { default } from "./DemoClient";
